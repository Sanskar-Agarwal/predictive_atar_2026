from django.urls import path
from . import views

urlpatterns = [
    #path('search_students/', views.search_students),
    # Add more patterns as needed
    #path('export_excel/',views.export_students_from_db_to_excel)

]